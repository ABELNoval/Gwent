using System;
using System.Collections.Generic;
using System.Diagnostics;
using UnityEngine;

namespace Script
{
    public class Debugs : MonoBehaviour
    {
        /*public static Lexer lexer = new Lexer("int a = 5; if (a > 4) { a = a + 1; }");
        private void Start()
        {
            List<Token> tokens = lexer.GetTokenList();
            for (int i = 0; i < tokens.Count; i++)
            {
                UnityEngine.Debug.Log($"{tokens[i].typeOfToken} => {tokens[i].value}");
            }
        }*/
    }
}